import React from "react";

const Rating = (props) => {
const show = () => {

        alert(props);

}
    return(
        <div>
            <button onClick={show}>ff</button>
            tessst
        </div>
    )
};

export default Rating;