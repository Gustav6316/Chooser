import React from "react";

const Rating = (props) => {
const show = () => {

        console.log(props.data);

}
    return(
        <div>
            <button onClick={show}>ff</button>
            tesss
        </div>
    )
};

export default Rating;